<div class="container w-75 p-3 d-flex align-content-start flex-wrap">
  <div class="col-4">
    <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">

      </div>
      <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
    </div>
  </div>
    <div class="d-flex align-content-start flex-wrap">
    <div class="jumbotron align-middle">
      <% if user_signed_in? %>
      <h1 class="display-4">Bienvenue, cher <%= @user.firstname.capitalize! %></h1>
      <% else %>
      <h1 class="display-4">Bienvenue à toi !</h1>
      <% end %>
      <p class="lead">La meilleure Pizzeria Italienne 🍕 livrée au bureau. Pizzas à base de véritables produits frais 🍅,  préparés à la minute par un Chef Pizaïolo 👨‍🍳 . Livraison en 20 minutes sur Paris.</p>
      <hr class="my-4">
      <% if !user_signed_in? %>
      <p>👇🏻 Pas encore de compte ? Va de suite t'en créer un pour profiter des meilleures Pizzas du monde!</p>
      <a class="btn btn-primary btn-lg" href="<%= new_user_registration_path %>" role="button">Go!</a>
      <% else %>
      <p>👇🏻Découvre notre carte !</p>
      <% end %>
    </div>
  </div>
</div>

<div class="container w-75 p-3">
  <div class="d-flex align-content-start flex-wrap">
    <%= render @items %>
  </div>
</div>
